# Environment Configuration

# Icecast Server Settings
# ICECAST_HOST - internal host for API calls (localhost when embedded)
ICECAST_HOST=localhost
# ICECAST_PUBLIC_HOST - what users see in connection info
ICECAST_PUBLIC_HOST=your-domain.com
ICECAST_PORT=8000
# Source password - must match icecast.xml
ICECAST_SOURCE_PASSWORD=streamdock_source

# API Server
PORT=3001

# Database
DATABASE_PATH=./server/stations.db

# Email Encryption (required for production SMTP password security)
ENCRYPTION_KEY=your-secure-random-32-char-key
